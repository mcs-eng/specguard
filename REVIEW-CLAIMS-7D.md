# SpecGuard Phase 7d-audit: claims audit and pre-publish sweep (checkpoint A1)

Date: 2026-08-26. Reviewer: the Phase 7d-audit session, working as a skeptical reader of this repository's own documentation, under the work order `specguard-work-order-7d-audit-2026-08.md`. Scope at this checkpoint: every load-bearing factual claim in `README.md`, `EVAL.md`, `DEVPOST.md`, `VIDEO-SCRIPT.md`, and the Phase 7c/7d sections of `HANDOFF.md`, checked against the code, the tests, the committed receipt files, and the live service; plus the pre-publish history sweep and a full-history secret scan. The Devpost form draft and the live demo dry run belong to checkpoint A2 and are not in this file. No feature, fixture, prompt, scorer, or runtime file was modified. Edits in this commit: four proven document corrections (rows C7-04, C7-05, C7-06, C7-07) and this file.

Audited tip: `4792f76`. Live serving revision at every live receipt in this file: `specguard-00030-jnr`. Vertex spend this session: zero; every live receipt is a read-only HTTP GET or a `gcloud` describe.

## Preconditions, verified before launch

1. **The final redeploy happened.** `gcloud run services describe specguard --region us-central1 --project specguard-hack --format="value(status.latestReadyRevisionName,status.traffic[0].percent,status.url)"` exit 0: `specguard-00030-jnr`, 100 percent, `https://specguard-ypkohkbwgq-uc.a.run.app`. The revision was created 2026-08-26T12:16:45Z from Cloud Build `a1950d32` (2026-08-26T12:15:33Z, SUCCESS), after the tip commit `4792f76` (2026-08-26T01:04:28Z). The revision env pins `SPECGUARD_AGENT_MODE=full_text` and `SPECGUARD_GEMMA_ENDPOINT=disabled`, exactly the `deploy-specguard.ps1` values at the tip; that script exists on no other branch, so the deploy came from this tree. Limit stated: a source deploy carries no commit stamp, so the tie to `4792f76` is the build timestamp after the tip, the exact env match, and the clean tree; the A2 dry run adds a behavioral check.
2. **The Gemma world is the sentinel-disabled world.** `SPECGUARD_GEMMA_ENDPOINT=disabled` on the live revision and in the deploy script. Every claim row below that depends on the severity path carries the marker `GEMMA-SENSITIVE`. If Mason enables Gemma on 08-29, exactly those rows re-check; otherwise the tags expire.

## Verdict up front

The load-bearing claims hold. Every published campaign number in `README.md` and `EVAL.md` traces to the 2026-08-25 `8900a76` campaign, and the committed `EVAL-RECEIPTS.jsonl` reproduces every tool-call figure exactly (R7-05). The offline containment projections stay only in `REVIEW-7D-MISSES.md`, labelled as projections. Detector claims say six detectors and `text_layer_integrity_v3` everywhere; no stale five-detector or v2 text survives outside historical board rows that describe the fix. The test count is real: 680 passed at the tip in this session's own run. Four defects were found and fixed in place: a stale deployed-revision line, a board row that presented first-campaign numbers as current without naming the campaign, a wrong case-run count in the video narration, and a Devpost sentence pointing at a record `EVAL.md` no longer carries.

One item needs Mason before the public flip and is not fixable inside this session's authority: a stray GitHub branch carries a commit authored by `Cursor Agent <cursoragent@cursor.com>` (row S7-08).

## Part 1: claims audit

Each row names the claim, the evidence checked, the verdict, and the action. Rows marked `GEMMA-SENSITIVE` re-check only if Mason enables the Gemma endpoint on 08-29.

| ID | Claim | Evidence checked | Verdict | Action |
| --- | --- | --- | --- | --- |
| C7-01 | Every number in the eval tables is the 2026-08-25 `8900a76` measurement (README generated block, EVAL.md all four sections). | Cross-check of every table cell and note against `EVAL-RECEIPTS.jsonl` and `EVAL-CONSOLE.log` (R7-05, R7-06); catch-rate arithmetic re-derived: `full_text` 80 of 90 case-runs = 89% headline, messy 60 of 70 = 86%, `navigate` messy 63 of 70 = 90%; per-pair tool-call means times runs equal the receipt totals in all ten rows. | Holds. | None. |
| C7-02 | No containment-projection number is published as measured. | The projection table (86%/89% class) exists only in `REVIEW-7D-MISSES.md`, prefixed "computed offline ... must not be published as measured". The measured 86% and 89% in README are the `8900a76` campaign's own harness numbers, coincidentally equal in one cell; both name their campaign. | Holds. | None. |
| C7-03 | The default-mode justification is consistent everywhere (supersedes the 13-A navigate wording, which the remeasure flip retired). | README lines 13, 204, 459; EVAL.md ship-gate section; DEVPOST "How it was built" and accomplishments; VIDEO honesty boundary. All state: gate followed at `31ef186`, `full_text` default, pre-commitment quoted, second gate informational, campaigns never pooled. The pre-commitment sentence is quoted identically in README and HANDOFF. | Holds. The work order's 13-A wording check is superseded by the remeasure flip; the decided-default story replaced it and is consistent. | None. |
| C7-04 | Live-service claims name the revision actually serving when checked. | README line 38 read "revision `specguard-00024-nst` as of 2026-08-22"; live revision at check time is `specguard-00030-jnr` (R7-02). `GET /health` on the README URL: HTTP 200 `ok`, 4.7 s cold, curl exit 0, against `specguard-00030-jnr`. | Was stale by six revisions. | FIXED — README now names `specguard-00030-jnr` as of 2026-08-26. A2 re-checks at dry-run time. |
| C7-05 | README board row 7c states E-13/E-15 remeasured results. | The row said E-13 "now measures 100% in navigate and 90% in full_text" and E-15 "now measures 60% and 80%" — those are `31ef186` numbers, unlabeled, while the current `8900a76` tables read E-13 100%/100% and E-15 0%/30%. The row contradicted README line 218 in the same file. | False as written: superseded numbers presented as current. | FIXED — the row now names both campaigns with each campaign's numbers. |
| C7-06 | VIDEO shot 5 narration: messy-lane gap is "four case-runs in seventy". | `navigate` 63 of 70 against `full_text` 60 of 70 (EVAL.md, R7-05): the gap is three case-runs. | False by one. | FIXED — "three case-runs in seventy". |
| C7-07 | DEVPOST: the 2026-08-22 live-Gemma record "is published beside" the current measurement. `GEMMA-SENSITIVE` | `EVAL.md` is regenerated in full and carries no 2026-08-22 section; that record lives in `HANDOFF.md` Phase 6e "The final evaluation" (EVAL SUMMARY date=2026-08-22, revision `a424ccf`, every finding HIGH). | False as written; the record moved. | FIXED — DEVPOST now says the superseded record stays receipted in `HANDOFF.md` Phase 6e. |
| C7-08 | Detector claims: six detectors, `text_layer_integrity_v3`, no stale five-detector or v2 text. | `specguard/integrity.py:166` `SCREEN_ID = "text_layer_integrity_v3"`; grep across the four published docs: "five detectors added after render mode 3" is arithmetically correct; the one `v2` mention is inside the FIXED board row describing the bump; DEVPOST "six detectors" and README "all six rules" consistent (R7-07). | Holds. | None. |
| C7-09 | Test-count lines match a real run at the audited tip. | This session: `uv run pytest -q` exit 0, `680 passed, 2 warnings` at `4792f76` (R7-01). README block records 680 at `19a31b1`, written by `scripts/record_test_count.py` from its own run; `19a31b1` is the parent of the recording commit, by design. | Holds. | None. |
| C7-10 | VIDEO shot 4a: `test_gate.py` plus `test_agent.py` pass count "68 on 2026-08-25". | This session: `uv run pytest -q tests/test_gate.py tests/test_agent.py` exit 0, `68 passed` (R7-08). | Holds. | None. |
| C7-11 | `EVAL-RECEIPTS.jsonl` holds 848 lines: 748 tool-call lines, 100 run summaries, one per audit; no write-tool call in either mode; all 372 `verify_quote` calls verified. | Recount from the file (R7-05): 848/748/100, 100 unique run ids, `full_text` = 40 `set_model_response` + 178 `verify_quote`, `navigate` = 1 `check_text_integrity` + 295 `extract_pdf_text` + 40 `set_model_response` + 194 `verify_quote`, zero `persist_finding`/`draft_rfi`, all 372 `response_verified: true`. Matches README, EVAL.md, and HANDOFF S4 cell for cell. | Holds. | None. |
| C7-12 | The four `EVAL SUMMARY` lines quoted in HANDOFF S2 are the campaign console's own. | `diff` of the four `EVAL SUMMARY` lines in `EVAL-CONSOLE.log` (115 lines, 4 summary lines) against the HANDOFF quotes: exit 0, byte-identical (R7-06). | Holds. | None. |
| C7-13 | README/DEVPOST core claims: `gate.py` is 184 lines; model is Gemini 3.7 Flash; ADK pinned 2.7.1; PyMuPDF pinned 1.28.2; parity test exists. | `wc -l specguard/gate.py` = 184; `specguard/agent.py:36` `MODEL_ID = "gemini-3.7-flash"`; `pyproject.toml` `google-adk==2.7.1`, `pymupdf==1.28.2`; `tests/test_navigate_mode.py:279` holds the parity test (R7-07). | Holds. | None. |
| C7-14 | Prompt-token and spend figures (7,435 / 152,425 / 31,750 / 274,535; 218 vs 305 calls; ~5.84M campaign tokens). | Means match EVAL.md; 218 = 70+148 from the receipts; 305 is the `31ef186` HANDOFF figure; 40×7435.1 + 10×152424.8 + 40×31749.7 + 10×274534.9 = 5,836,989 ≈ 5.84M. | Holds. | None. |
| C7-15 | EVAL provenance: "regenerated on 2026-08-25 by the harness at code revision `8900a76`". | The numbers were measured live at `8900a76` (d98e813 committed the harness output); the current file bytes are the `d6804d4` offline re-render under the corrected informational-gate renderer, proven byte-exact for unchanged sections and disclosed in HANDOFF S3 and "What this phase leaves for Mason" item 2. | Holds with the HANDOFF disclosure; the README sentence compresses the re-render step. | Recorded, not changed: the disclosure exists where the receipts live, and the next live campaign regenerates normally. |
| C7-16 | Severity claims: advisory label, recorded fallback, sentinel reason text, two provenance fields, endpoint torn down outside windows. `GEMMA-SENSITIVE` | `specguard/severity.py`, README Gemma section, EVAL.md header lines 7–8, DEVPOST honesty paragraph, VIDEO teardown step 2; live env `SPECGUARD_GEMMA_ENDPOINT=disabled`; `gcloud ai endpoints list` receipts in HANDOFF show teardown. | Holds in the sentinel-disabled world. | Re-check tagged rows only if Gemma is enabled 08-29. |
| C7-17 | VIDEO claims that depend on live behavior: run-page wording, RFI header shape, `/gate` prefill, sample-run presentation. `GEMMA-SENSITIVE` (3c badge only) | Not checkable from documents alone; HANDOFF (presentation-polish "What this phase leaves for Mason") already records that stored sample runs predate the submittal-number presentation and render the legacy fallback until they are recreated after the deploy. | Deferred by design. | A2 dry run walks every scene against `specguard-00030-jnr` and records each mismatch as a finding for Mason. |
| C7-18 | VIDEO shoot-day checklist runs from `C:\Users\mcspd\dev\specguard`. | That clone is `main`, which lacks the phase-7d code until Mason's ff-only merge. The checklist is written for the post-merge world. | Holds conditionally. | Recorded: the merge precedes the shoot in Mason's queue; no change. |
| C7-19 | HANDOFF Phase 7c/7d sections' quality-gate and campaign receipts. | Phase-by-phase pytest counts (463 → 613 → 620 → 636 → 657 → 680) are internally consistent and the endpoint value 680 reproduces at the tip; commit hashes named in the sections all exist in history; the S4 receipt table reproduces from the committed file. | Holds. | None. |
| C7-20 | DEVPOST latency bullets (10.7 s / 12.2 s / 48.1 s) and pacing table. `GEMMA-SENSITIVE` (warm-revision rows) | Labelled with their 2026-08-21 receipt (REVIEW-CLAIMS.md R-12); VIDEO T-1 step 2 replaces them with a fresh timed dry run before the shoot. | Holds as labelled. | None; the T-1 dry run refreshes them. |

## Part 2: pre-publish sweep

All commands ran in `C:\Users\mcspd\dev\specguard-7d` on arya at `4792f76`. `git rev-list --all` counts 125 revisions.

| ID | Check | Result | Action |
| --- | --- | --- | --- |
| S7-01 | `git log --all --oneline -- SETUP.md PLAN.md` | Empty, exit 0. `git ls-files` empty for both; `.gitignore` lines 16–17 carry both. | None. |
| S7-02 | Scrubbed sensitive values across all 125 revisions: the billing account id, the budget id, the two personal addresses, the two unrelated project slugs. | `git grep` per value across `git rev-list --all`: zero hits for every value (R7-03). The values were read from Mason's local `SETUP.md` and are not quoted here. | None. |
| S7-03 | Key-shaped strings across all revisions (Google API key with tail, private-key blocks, OAuth/GitHub/AWS/Slack token shapes). | One hit: the R-04 grep-pattern receipt line inside `REVIEW-CLAIMS.md`, which quotes the patterns as literals. The bare `AIza` literal appears only in the two review records as grep-receipt text (206 rev-hits, two files). Benign (R7-04). | None. |
| S7-04 | Full-history secret scan, `gitleaks` equivalent via `uv tool run`. | `gitleaks` is not installed; `detect-secrets` (Yelp) ran via `uv tool run` over all 508 unique non-PDF blobs in history, extracted with `git cat-file` (R7-04). Findings: (a) one Private-Key flag on the same R-04 grep-pattern receipt line — benign; (b) Hex-High-Entropy flags in `EVAL-RECEIPTS.jsonl` — every one is a `quote_sha256` digest or a uuid4 `run_id`, provenance of fictional fixtures, spot-checked against the raw lines — benign. Zero credential findings. | None. |
| S7-05 | Fixture names, people, companies, values fictional. | `fixtures/MANIFEST.md` states all-fictional; the exact-name collision table (five searches, 2026-08-22) is present for the 7c names; the Phase 2 names carry their collision checks in HANDOFF Phase 2. No real vendor, client, PDG, or PEL string in any fixture or manifest. | None. |
| S7-06 | AI attribution in commit messages. | `git log --all` grep for `Co-Authored-By`, `Generated with`, `noreply@anthropic`: zero hits in commit messages on `phase-7d` and `main` (R7-09). | None. |
| S7-07 | Commit authors. | Two identities for Mason (`masonsnider94@gmail.com`, `275487979+mcs-eng@users.noreply.github.com`), both named `mcs-eng` — fine. One foreign identity: see S7-08. | See S7-08. |
| S7-08 | **Stray AI-authored commit on the GitHub remote.** | Commit `faec4fd`, author and committer `Cursor Agent <cursoragent@cursor.com>`, trailer `Co-authored-by: mcs-eng <mcs-eng@users.noreply.github.com>`, adds a benign 4-line `.cursor/environment.json` on branch `origin/cursor/setup-dev-environment-d57b` (parented on `main` at `187f430`). It is on no local branch. Content is harmless; the authorship violates the standing no-AI-attribution rule and becomes public with the repo flip, and the stray branch ships with it. | **STOP row for Mason (D7-1).** This session's push authority covers backup pushes of `phase-7d` only, so it deleted nothing. |
| S7-09 | Secret scanning on the GitHub repo. | Still unavailable on the private repo (HANDOFF Phase 1 receipt); the enable-after-flip step stays in Mason's queue unchanged. | None here. |

## Part 3: rows for Mason

- **D7-1 (decision, STOP row): the Cursor Agent branch.** Before the public flip, delete `cursor/setup-dev-environment-d57b` from GitHub, or accept an AI-authored commit becoming public against the standing rule. Deletion is one command and destroys nothing local; the commit adds only a 4-line Cursor environment file. arya (PowerShell): `git -C C:\Users\mcspd\dev\specguard push origin --delete cursor/setup-dev-environment-d57b` then `git -C C:\Users\mcspd\dev\specguard-7d fetch --prune`. Re-run the S7-04 scan gate afterwards only if anything else changes; the commit contains no sensitive value, so the sweep verdicts above do not depend on this decision.
- **D7-2 (recorded, no decision needed): the EVAL re-render provenance** (C7-15) stands on the HANDOFF disclosure. If Mason wants the compression removed, one clause in the README provenance paragraph does it; the audit did not change generated-adjacent text without need.
- **The `GEMMA-SENSITIVE` rows** are C7-07, C7-16, C7-17 (badge beat), C7-20 (warm rows). They re-check only if the 08-29 decision enables the endpoint.

## Receipts

Every exit code is from the unpiped command shown. Live receipts name the serving revision `specguard-00030-jnr`.

| ID | Command | Exit | Result |
| --- | --- | ---: | --- |
| R7-01 | `uv run pytest -q`; `uv run ruff check .`; `uv run ruff format --check .`; `git diff --check`; `git status --porcelain` at `4792f76` | 0 each | `680 passed, 2 warnings`; `All checks passed!`; `57 files already formatted`; clean; empty. |
| R7-02 | `gcloud run services describe specguard --region us-central1 --project specguard-hack --format="value(status.latestReadyRevisionName,status.traffic[0].percent,status.url)"`; revision describe (creation, env); `gcloud builds list --limit 3` | 0 each | `specguard-00030-jnr`, 100, created 2026-08-26T12:16:45Z; env pins `full_text` and sentinel `disabled`; build `a1950d32` 2026-08-26T12:15:33Z SUCCESS. |
| R7-03 | `git grep "<value>" $(git rev-list --all)` per scrubbed value (billing id, budget id, two addresses, two project slugs); `git log --all -- SETUP.md PLAN.md`; `git ls-files SETUP.md PLAN.md` | 0 per call | Zero hits per value across 125 revisions; both history gates empty; neither file tracked. |
| R7-04 | Key-shape `git grep -E` across all revisions; `uv tool run detect-secrets scan` over 508 extracted unique blobs | 0 each | One grep-pattern-receipt hit (benign); detect-secrets: 2 files flagged, both triaged benign (grep-receipt line; fixture digests and run ids). |
| R7-05 | Recount of `EVAL-RECEIPTS.jsonl` (Python over the committed file) | 0 | 848 lines = 748 calls + 100 summaries; 100 unique runs; per-mode per-tool counts match every published table; zero write-tool calls; 372 `verify_quote` all `response_verified: true`. |
| R7-06 | `diff` of `EVAL SUMMARY` lines: `EVAL-CONSOLE.log` against HANDOFF S2 | 0 | Byte-identical, four lines. Console file: 115 lines. |
| R7-07 | `wc -l specguard/gate.py`; grep `MODEL_ID`, `google-adk`, `SCREEN_ID`, parity-test name; stale-text greps over the four published docs | 0 each | 184; `gemini-3.7-flash`; `==2.7.1`; `text_layer_integrity_v3`; test at `tests/test_navigate_mode.py:279`; no stale detector or default text outside labelled history. |
| R7-08 | `uv run pytest -q tests/test_gate.py tests/test_agent.py` | 0 | `68 passed, 1 warning`. |
| R7-09 | `git log --all --format='%an %ae' \| sort -u`; `git log --all -E --grep='Co-Authored-By\|Generated with\|noreply@anthropic'` | 0 each | Three identities (two are Mason's); zero attribution hits in messages; the Cursor Agent author is S7-08. |
| R7-10 | `curl.exe -s -o <file> -w "%{http_code} %{time_total}" https://specguard-108657628939.us-central1.run.app/health` | 0 | HTTP 200, body `ok`, 4.72 s (cold start), against `specguard-00030-jnr`. |
